# --- Doxygen artifacts ---
latex/
docs/latex/
*.log
doxygen_sqlite3.db

# --- IDE & System ---
.idea/
.vscode/
.DS_Store
__pycache__/
*.pyc

# --- Python venvs & local env ---
venv/
env/
.venv/
.env
*.egg-info/

# --- Models (binary weights & per-run artefacts; metadata stays tracked) ---
saved_models/*.pth
saved_models/archive/
saved_models/train*
saved_models/charter_smoke*
saved_models/history_metrics.json

# --- Benchmarks (runtime outputs) ---
benchmarks/history.json
benchmarks/reports/

# --- Datasets ---
# Big or generated files stay out of the repo;
# curated cold-start anchors are explicitly allowed below.
data/processed/
data/cleaned_dataset.npy
data/cleaned_dataset.charter.npy
data/raw/drop_zone/
data/raw/presets/
data/raw/venv/
data/raw/__pycache__/
data/raw/*.txt
data/raw/*.npy
!data/raw/anchor_presets.json

# --- Metrics dashboard runtime artefacts ---
metrics_dashboard/events/
metrics_dashboard/_uploaded/
metrics_dashboard/.env
metrics_dashboard/.env.local
metrics_dashboard/stats_snapshot.json
metrics_dashboard/latest_metrics.json
metrics_dashboard/models.json
metrics_dashboard/presets.json
metrics_dashboard/index.json
metrics_dashboard/__pycache__/
# Allow source files of the dashboard (index.html, receiver.php, api.php,
# .htaccess, README.md, push_metrics.*, history_metrics.json) by NOT listing them.
